//
//  ViewControllerNewMonthlyGoal.swift
//  outOfMatrix
//
//  Created by garzoneroberto on 08/10/18.
//  Copyright © 2018 TheJagers. All rights reserved.
//

import UIKit

class ViewControllerNewMonthlyGoal: UIViewController {

    override func viewDidLoad() {
        super.viewDidLoad()

        // Do any additional setup after loading the view.
    }
    
/*    override func prepare(for segue: UIStoryboardSegue, sender: Any?) {
        if (segue.identifier == "segueToWeekly") {
            let vc = segue.destination as! ViewControllerNewWeeklyGoal
        }
        else if (segue.identifier == "segueToMpnthly") {
            let vc = segue.destination as! ViewControllerNewMonthlyGoal
        }
    } */

    /*
    // MARK: - Navigation

    // In a storyboard-based application, you will often want to do a little preparation before navigation
    override func prepare(for segue: UIStoryboardSegue, sender: Any?) {
        // Get the new view controller using segue.destination.
        // Pass the selected object to the new view controller.
    }
    */

}
